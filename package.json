{
  "name": "CI_CD_lab14",
  "version": "1.0.0",
  "description": "",
  "main": "project.js",
  "scripts": {
    "format": "prettier --write .",
    "start": "node project.js",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "jest",
    "coverage": "jest --coverage --coverageReporters=cobertura --coverageDirectory=coverage",
    "test:integration": "jest --config jest.integration.config.js"
  },
  "private": true,
  "devDependencies": {
    "eslint": "^8.0.0",
    "globals": "^16.2.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "supertest": "^7.1.1",
    "jest-junit": "^16.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
