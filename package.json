{
  "name": "lab12",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "coverage": "jest --coverage --coverageReporters=cobertura --coverageDirectory=target/reports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "sonarqube-scanner": "^4.3.0",
    "supertest": "^7.1.1"
  },
  "jest-junit": {
    "outputDirectory": "target/reports",
    "outputName": "jest-results.xml"
  }
}
